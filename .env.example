# Required for operator-note interpretation.
OPENAI_API_KEY=replace_with_your_api_key

# Fast/low-cost default. Change to another compatible model if desired.
OPENAI_MODEL=gpt-5.6-luna

LOG_LEVEL=INFO
